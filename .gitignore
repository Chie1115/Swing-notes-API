# Node.js dependency folder (do NOT commit node_modules)
node_modules/

# Environment variables (contains secrets, never commit)
.env

# Log files generated by the app or Node
*.log

# Optional: Build or cache folders (if you add any build step later)
dist/
build/
.cache/

# Optional: OS-specific or editor files you want to ignore
.DS_Store
.vscode/
